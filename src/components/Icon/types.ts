import { ColorTheme } from "../Navigation/NavigationLink/types";

export type IconProps = {
  id: string;
  fill?: string;
  stroke?: string;
  width?: number | string;
  height?: number | string;
  type?: ColorTheme;
};
